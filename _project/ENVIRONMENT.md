# ENVIRONMENT — sappchoir

<!-- UPDATE WHEN: an env var is added/renamed/removed -->

No environment variables, no .env, no secrets. Build-time knobs are CMake
options (`SAPPCHOIR_BUILD_PLUGIN/TESTS/CLI`, `SAPPSOUNDS_DIR`,
`FETCHCONTENT_SOURCE_DIR_JUCE`).
