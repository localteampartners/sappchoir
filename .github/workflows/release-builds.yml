name: release-builds

# Builds VST3/AU/Standalone for macOS (universal) and VST3/Standalone for
# Windows, and attaches the zips to the GitHub release for the given tag.
# Runs on tag push, or manually (Actions -> release-builds -> Run workflow)
# against an existing release tag.

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing release tag to attach builds to (e.g. v0.1.0)"
        required: true

permissions:
  contents: write

jobs:
  macos:
    runs-on: macos-14
    env:
      TAG: ${{ inputs.tag || github.ref_name }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: >
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64"
          -DSAPPCHOIR_BUILD_TESTS=OFF
      - name: Build
        run: cmake --build build --config Release -j3
      - name: Package
        run: |
          ART="build/SappChoirPlugin_artefacts/Release"
          STAGE="SappChoir-$TAG-macOS-universal"
          mkdir -p "$STAGE"
          cp -R "$ART/VST3/SappChoir.vst3" "$STAGE/"
          cp -R "$ART/AU/SappChoir.component" "$STAGE/"
          cp -R "$ART/Standalone/SappChoir.app" "$STAGE/"
          cp "build/sappchoir" "$STAGE/" || true
          cat > "$STAGE/INSTALL.txt" <<'EOF'
SappChoir — macOS (universal: Apple Silicon + Intel)

1. Copy SappChoir.vst3      -> ~/Library/Audio/Plug-Ins/VST3/
2. Copy SappChoir.component -> ~/Library/Audio/Plug-Ins/Components/
3. SappChoir.app is the standalone version.

Unsigned build: after copying, clear quarantine once:
  xattr -rc ~/Library/Audio/Plug-Ins/VST3/SappChoir.vst3 ~/Library/Audio/Plug-Ins/Components/SappChoir.component
Then rescan plugins in your DAW.
EOF
          zip -qry "$STAGE.zip" "$STAGE"
      - name: Upload to release
        run: |
          gh release view "$TAG" >/dev/null 2>&1 || gh release create "$TAG" --generate-notes || true
          gh release upload "$TAG" "SappChoir-$TAG-macOS-universal.zip" --clobber

  windows:
    runs-on: windows-latest
    env:
      TAG: ${{ inputs.tag || github.ref_name }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake -S . -B build -DSAPPCHOIR_BUILD_TESTS=OFF
      - name: Build
        run: cmake --build build --config Release
      - name: Package
        shell: pwsh
        run: |
          $art = "build/SappChoirPlugin_artefacts/Release"
          $stage = "SappChoir-$env:TAG-Windows-x64"
          New-Item -ItemType Directory -Path $stage | Out-Null
          Copy-Item -Recurse "$art/VST3/SappChoir.vst3" "$stage/"
          Copy-Item "$art/Standalone/SappChoir.exe" "$stage/"
          Copy-Item "build/Release/sappchoir.exe" "$stage/" -ErrorAction SilentlyContinue
          @"
SappChoir — Windows x64

1. Copy the SappChoir.vst3 folder to: C:\Program Files\Common Files\VST3\
2. SappChoir.exe is the standalone version — run it from anywhere.
Then rescan plugins in your DAW.
"@ | Set-Content "$stage/INSTALL.txt"
          Compress-Archive -Path $stage -DestinationPath "$stage.zip"
      - name: Upload to release
        shell: pwsh
        run: |
          gh release view "$env:TAG" 2>$null; if ($LASTEXITCODE -ne 0) { gh release create "$env:TAG" --generate-notes }
          gh release upload "$env:TAG" "SappChoir-$env:TAG-Windows-x64.zip" --clobber
